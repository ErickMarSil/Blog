import {MainElement} from './App';
import ReactDOM from 'react-dom/client';
import React from 'react';

const root = ReactDOM.createRoot(document.getElementById("root"))
const arr = ['banana', 'maçã', 'pessego', 'laranja']

root.render(
  <MainElement />
)